# =============================================================================
# THE CALISTA BACKEND - ENVIRONMENT VARIABLES
# Only variables actually referenced in the codebase
# =============================================================================

# -----------------------------------------------------------------------------
# Core Runtime Configuration
# -----------------------------------------------------------------------------
# Server port (usually set automatically by hosting platforms like Railway)
# PORT=3001

# Environment mode - affects error messages, cookie security, etc.
NODE_ENV=production

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
# Frontend/client URLs for CORS allowlist
FRONTEND_URL=https://your-frontend-domain.com
CLIENT_URL=https://your-frontend-domain.com

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# MongoDB connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/thecalista

# -----------------------------------------------------------------------------
# Authentication
# -----------------------------------------------------------------------------
# Secret key for signing JWT tokens (use a strong random string)
JWT_SECRET=your-super-secure-jwt-secret-key-here

# JWT token expiration duration (e.g., 7d, 24h, 30m)
JWT_EXPIRE=7d

# -----------------------------------------------------------------------------
# Supabase Configuration
# -----------------------------------------------------------------------------
# Supabase project URL
SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase anonymous/public key (used for client operations with RLS)
SUPABASE_ANON_KEY=your-supabase-anon-key

# Supabase service role key (admin access, bypasses RLS - HIGHLY SENSITIVE)
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# -----------------------------------------------------------------------------
# Feature Flags (Optional - only used for status reporting)
# -----------------------------------------------------------------------------
# Enable OAuth features (currently not implemented)
OAUTH_ENABLED=false

# Enable personalized AI features (currently not implemented)
PERSONALIZED_AI_ENABLED=false

# Google OAuth client ID (currently only used for status logging)
# GOOGLE_CLIENT_ID=your-google-client-id

# Gemini API key (currently only used for status logging)
# GEMINI_API_KEY=your-gemini-api-key
# Frontend env example
# Set your API base URL if the frontend talks to the backend
VITE_API_URL=http://localhost:3001

# Any public keys or flags can go here, e.g. Stripe public key
VITE_STRIPE_PUBLIC_KEY=
